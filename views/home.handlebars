<section>

    <div class="container">
        <div class="row">
            <div class="col-md-12"><img src="http://placehold.it/350x250"></div>
        </div>
        <div class="row">
            {{#each docs}}
            <div class="col-md-4">{{@index}}. {{username}} - {{email}}</div>
            {{/each}}
        </div>
    </div>

</section>